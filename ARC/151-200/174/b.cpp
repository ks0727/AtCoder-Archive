#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)

int main(){
    int t;
    cin >>t;
    rep(ti,t){
        vector<int> a(5);
        rep(i,5) cin >> a[i];
        vector<int> p(5);
        rep(i,5) cin >> p[i];
    }
    return 0;
}