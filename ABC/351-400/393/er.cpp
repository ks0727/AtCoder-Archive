#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<n;++i)
const int M = 1e6+5;

int main(){
    int n,k;
    cin >> n >> k;
    vector<int> a(n);
    rep(i,n) cin >> a[i];
    vector<int> cnt(M);
    for(int i=2;i<M;i++){
        for(int j=i*i;j<M;j+=i){
            
        }
    }
    return 0;
}   