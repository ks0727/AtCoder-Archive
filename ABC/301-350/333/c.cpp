#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
using ll = long long;

int main(){
    int n;
    cin >> n;
    string repu = "";
    repu += '3';
    vector<vector<string> > nums;
    nums[0].push_back(repu);
    for(int i=1;i<=(12);i++){
        rep(j,nums[i-1].size()){
            string s = nums[i-1][j];
            s.insert()
            nums[i].push_back()
        }
    }
    return 0;
}