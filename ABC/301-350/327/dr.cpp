#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)

int main(){
    int n,m;
    cin >> n >> m;
    vector<int> a(m),b(m);
    rep(i,m) cin >> a[i];
    rep(i,m) cin >> b[m];
    bool bipartite = false;
    auto dfs = [&](auto dfs,int v,int prev){
        
    };
}