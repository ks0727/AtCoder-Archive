#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)
using P = pair<int,int>;

int main(){
    int w,h;
    cin >> w >> h;
    int n;
    cin >> n;
    vector<int> p(n),q(n);
    rep(i,n) cin >> p[i] >> q[i];
    int A,B;
    cin >> A >> B;
    vector<int> a(A),b(B);
    rep(i,A) cin >> a[i];
    rep(i,B) cin >> b[i];
    for(auto y : b){
        for(auto x : a){
            
        }
    }
}