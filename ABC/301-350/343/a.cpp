#include <bits/stdc++.h>
using namespace std;
#define rep(i,n) for(int i=0;i<(n);i++)

int main(){
    int a,b;
    cin >> a >> b;
    if(a == 0 && b == 0) cout << 1 << endl;
    else cout << 0 << endl;
    return 0;
}